<a href="/users/<%=user.id-%>" class="user-preview" id="user<%=user.id%>">
  <img class="image" src="<%= user.photo %>" alt="user" />
  <div class="info">
    <h2><%= user.name %></h2>
    <sub>Number of posts <%= user.posts.size %></sub>
  </div>
</a>
